//! Node policy engine: config validation, automod rule loading, branding
//! (docs/05, docs/10). Branding must never be able to reach the
//! encryption-tier indicators or the Hall/private visual distinction — a
//! hostile operator styling an unencrypted Hall to look encrypted is the
//! one thing this boundary exists to prevent (docs/06).
