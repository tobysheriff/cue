//! zkgroup-style anonymous membership credentials and Privacy Pass/VOPRF
//! blind delivery tokens (docs/03, docs/04 #4). Lands in Phase 2/3 (docs/11).

use crate::CryptoError;

pub fn issue_credential() -> Result<(), CryptoError> {
    Err(CryptoError::NotImplemented("credentials::issue_credential"))
}
